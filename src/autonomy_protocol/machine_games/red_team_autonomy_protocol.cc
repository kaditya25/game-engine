#include "red_team_autonomy_protocol.h"

namespace game_engine {

    std::unordered_map<std::string, Trajectory>
    RedTeamAutonomyProtocol::UpdateTrajectories() {

        // RED TEAM: FILL OUT THIS FUNCTION

        return std::unordered_map<std::string, Trajectory>();
    }
}
